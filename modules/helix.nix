_: {
  programs.helix = {
    enable = true;
    settings = {
      editor.lsp.display-messages = true;
      theme = "onedark";
      # keys.normal = {
      #   space.space = "file_picker";
      #   space.w = ":w";
      #   space.q = ":q";
      # };
    };
    # themes = {  };
  };
}
